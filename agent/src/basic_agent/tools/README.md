# tools/

Reserved for future Agent tool implementations (file ops, shell, web fetch, etc.).
Each tool will register itself with a central registry consumed by future
`ToolCallingAgent` / `ReActAgent` implementations under `agents/`.

Empty by design at this stage.
